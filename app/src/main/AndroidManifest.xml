<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.active">
<uses-permission android:name="android.permission.READ_CONTACTS"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/crack"
        android:label="Rescue"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Db_conent"
            android:parentActivityName=".About"></activity>
        <activity android:name=".Searchpage" />
        <activity android:name=".locationpage" />
        <activity android:name=".About" />
        <activity android:name=".Search" />
        <activity android:name=".Location" />
        <activity android:name=".next" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>